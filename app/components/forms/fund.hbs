{{#if this.errors}}
  <div class="callout alert">
    {{#each this.errors as |error|}}
      <p>{{error}}</p>
    {{/each}}
  </div>
{{/if}}
<form {{on "submit" this.save}}>
  <label for="fund-name-input">Name</label>
  <Input @value={{@fund.name}} @type="text" id="fund-name-input" aria-required="true" />

  <Input @value="Save" @type="submit" id="fund-submit" />
</form>
