<nav class="secondary">
  <ul>
    <li>
      {{#if this.isDetailsRoute}}
        <span>
          <FaIcon @icon="list" @size="sm" />
          Details
        </span>
      {{else}}
        <LinkTo @route="expenses.show" @model={{@expenseId}}>
          <FaIcon @icon="list" @size="sm" />
          Details
        </LinkTo>
      {{/if}}
    </li>
    <li>
      {{#if this.isAttachmentsRoute}}
        <span>
          <FaIcon @icon="paperclip" @size="sm" />
          Attachments
        </span>
      {{else}}
        <LinkTo @route="expenses.attachments" @model={{@expenseId}}>
          <FaIcon @icon="paperclip" @size="sm" />
          Attachments
        </LinkTo>
      {{/if}}
    </li>
    <li>
      {{#if this.isSettingsRoute}}
        <span>
          <FaIcon @icon="gear" @size="sm" />
          Settings
        </span>
      {{else}}
        <LinkTo @route="expenses.settings" @model={{@expenseId}}>
          <FaIcon @icon="gear" @size="sm" />
          Settings
        </LinkTo>
      {{/if}}
    </li>
  </ul>
</nav>
