{{title category.name}}
<div class="grid-container">
  <div class="grid-x">
    <div class="cell">
      <h1>{{category.name}}</h1>
    </div>
  </div>
  <div class="grid-x">
    <div class="cell">
      <ul class="menu">
        <li>
          {{#link-to "categories.show" category.id}}
            {{fa-icon "list"}} Details
          {{/link-to}}
        </li>
        <li>
          {{#link-to "categories.subcategories" category.id}}
            {{fa-icon "project-diagram"}} Subcategories
          {{/link-to}}
        </li>
        <li class="is-active">
          {{#link-to "categories.expenses" category.id}}
            {{fa-icon "credit-card"}} Expenses
          {{/link-to}}
        </li>
      </ul>
    </div>
  </div>
  <div class="grid-x">
    <div class="cell">
      <a id="show-create-expense-form-button" class="button" {{action "showCreateForm"}}>
        Create
      </a>
    </div>
  </div>
  <div class="grid-x">
    <div class="cell">
      {{#if expenses.length}}
        {{pagination-table rows=expenses columns=tableColumns limit=limit page=page meta=meta}}
      {{else}}
        No expenses exist here
      {{/if}}
    </div>
  </div>
  {{#if newExpense}}
    <div class="overlay">
      <div class="grid-x grid-padding-x align-center modal">
        <div class="cell small-12 medium-10 large-8 modal-cell">
          {{forms/expense/create-expense expense=(changeset newExpense ExpenseValidations) expenseCreated=(action "closeCreateForm") vendorSelected=(action "vendorSelected")}}
        </div>
      </div>
    </div>
  {{/if}}
</div>
