{{#if errors}}
  <div class="grid-x">
    <div class="cell">
      <p class="alert">
        {{#each errors as |error|}}
          {{error}}
        {{/each}}
      </p>
    </div>
  </div>
{{/if}}
<div class="grid-x">
  <div class="cell">
    <form>
      <div class="grid-x">
        <div class="cell">
          {{forms/validated-input type="text" changeset=vendor propertyName="name" label="Vendor Name" propertyId="edit-vendor-name"}}
        </div>
      </div>
      <div class="grid-x">
        <div class="cell">
          <input type="submit" id="save-vendor-button" disabled={{disableSubmit}} class="button" value="Save" {{action "submit"}}>
        </div>
      </div>
    </form>
  </div>
</div>
