<div class="container-lg">
  <h1>Funds</h1>
  <PaginatedTable
    @columns={{this.tableColumns}}
    @rows={{this.funds}}
    @page={{this.page}}
    @numPages={{this.funds.meta.pages}}
    @setPage={{this.setPage}}
    @headerButtons={{this.paginationButtons}}
  />
</div>
