<div class="container-lg">
  <div class="title-with-buttons">
    <h1>Funds</h1>
    <div class="buttons">
      <LinkTo @route="funds.new" class="button">
        New
      </LinkTo>
    </div>
  </div>
  <PaginatedTable
    @columns={{this.tableColumns}}
    @rows={{this.funds}}
    @page={{this.page}}
    @numPages={{this.funds.meta.pages}}
    @setPage={{this.setPage}}
  />
</div>
