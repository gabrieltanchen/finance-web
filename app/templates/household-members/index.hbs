<div class="container-lg">
  <div class="title-with-buttons">
    <h1>Members of Household</h1>
    <div class="buttons">
      <LinkTo @route="household-members.new" class="button">
        New
      </LinkTo>
    </div>
  </div>
  <PaginatedTable
    @columns={{this.tableColumns}}
    @rows={{this.householdMembers}}
    @page={{this.page}}
    @numPages={{this.householdMembers.meta.pages}}
    @setPage={{this.setPage}}
  />
</div>
