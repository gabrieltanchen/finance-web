<div class="container-lg">
  <h1>View Income</h1>
  <Menus::IncomeItem @incomeId={{this.income.id}} />
</div>
<div class="container-sm">
  <LinkTo @route="income.edit" @model={{this.income.id}} class="button">
    Edit
  </LinkTo>
  <button type="button" class="button alert" {{on "click" this.openDeleteModal}}>
    Delete
  </button>
</div>
{{#if this.showDeleteModal}}
  <div class="overlay">
    <div class="container-sm modal">
      {{#if this.deleteErrors}}
        <div class="callout alert">
          {{#each this.deleteErrors as |error|}}
            <p>{{error}}</p>
          {{/each}}
        </div>
      {{/if}}
      <p>Are you sure you want to delete this income?</p>
      <button type="button" class="button alert" {{on "click" this.deleteIncome}}>
        Delete
      </button>
      <button type="button" class="button cancel" {{on "click" this.closeDeleteModal}}>
        Cancel
      </button>
    </div>
  </div>
{{/if}}
