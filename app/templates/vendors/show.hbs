{{title vendor.name}}
<div class="grid-container">
  <div class="grid-x">
    <div class="cell small-8">
      <h1>{{vendor.name}}</h1>
    </div>
    <div class="cell small-4">
      <div class="button-group float-right">
        {{#link-to "vendors.edit" vendor.id class="primary button"}}
          Edit
        {{/link-to}}
        <button id="delete-vendor-button" class="alert button" {{action "openDeleteModal"}}>
          Delete
        </button>
      </div>
    </div>
  </div>
  {{#if errors}}
    <div class="grid-x">
      <div class="cell">
        <div class="callout alert">
          {{#each errors as |error|}}
            <p>{{error}}</p>
          {{/each}}
        </div>
      </div>
    </div>
  {{/if}}
  <div class="grid-x">
    <div class="cell">
      <ul class="menu">
        <li class="is-active">
          {{#link-to "vendors.show" vendor.id}}
            {{fa-icon "list"}} Details
          {{/link-to}}
        </li>
        <li>
          {{#link-to "vendors.expenses" vendor.id}}
            {{fa-icon "credit-card"}} Expenses
          {{/link-to}}
        </li>
      </ul>
    </div>
  </div>
  <div class="grid-x">
    <div class="cell">
      <table class="unstriped">
        <thead>
          <tr>
            <th>Property</th>
            <th>Value</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>UUID</td>
            <td>{{vendor.id}}</td>
          </tr>
          <tr>
            <td>Name</td>
            <td>{{vendor.name}}</td>
          </tr>
          <tr>
            <td>Created At</td>
            <td>{{vendor.created_at}}</td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>
{{#if showDeleteModal}}
  <div class="overlay">
    <div class="grid-x grid-padding-x align-center modal">
      <div class="cell small-12 medium-10 large-8 modal-cell">
        <p>Are you sure you want to delete <em>{{vendor.name}}</em>?</p>
        <div class="button-group">
          <button id="confirm-delete-vendor-button" class="alert button" {{action "deleteVendor"}}>
            Delete
          </button>
          <button id="cancel-delete-vendor-button" class="secondary button" {{action "closeDeleteModal"}}>
            Cancel
          </button>
        </div>
      </div>
    </div>
  </div>
{{/if}}
